 
# Signoff

Status: Closed

Before reading the explainer, I did not understand how DPO differs from supervised fine-tuning at the optimization level, nor why it might fail to improve evaluation scores.

After reading the revised explainer, I now understand that DPO optimizes relative preference probabilities rather than absolute correctness, which explains why improvements may not appear in evaluation benchmarks if the objectives are misaligned.

The explanation fully addressed my question.